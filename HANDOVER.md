# Claude Code への引き継ぎ計画

**対象プロジェクト**：レヴィン行動理論による特性診断ツール
**引き継ぎ元**：Claude（チャット環境。作業ディレクトリは毎回消える）
**引き継ぎ先**：Claude Code（ローカル環境。ファイルが永続する）

---

## この文書の使い方

- **第1節** は、あなた（人間）が手を動かす部分です。ここだけ読めば移行できます
- **第2節以降** は、Claude Code に読ませるための情報です。移行後、
  Claude Code に「HANDOVER.md を読んで」と伝えれば把握できます

---

# 第1節　あなたにやっていただくこと

## 手順1　ファイルを受け取る

このチャットの出力から `lewin-behavior-diagnostic` フォルダ一式をダウンロードし、
手元の作業したい場所に置いてください。

```
lewin-behavior-diagnostic/
├── HANDOVER.md          ← この文書
├── SKILL.md
├── assets/              （4ファイル）
├── references/          （4ファイル）
├── scripts/             （4ファイル）
└── tests/               （8ファイル）
```

**フォルダ名は変えても構いません。** スクリプトは自分の位置から
プロジェクトルートを求めるので、どこに置いても動きます。

## 手順2　動く環境を用意する

必要なものは3つです。

| 必要なもの | 用途 | 確認コマンド |
|---|---|---|
| Python 3 | ビルドと採点 | `python3 --version` |
| Node.js | 生成したJSの検証 | `node --version` |
| Playwright（任意） | 印刷PDFの検証 | `python3 -c "import playwright"` |

Playwright は印刷まわりを触るときだけ必要です。入れる場合：

```bash
pip install playwright
playwright install chromium
```

`pdftotext`（poppler-utils）も印刷検証で使います。
macOS なら `brew install poppler`、Ubuntu なら `apt install poppler-utils` です。

## 手順3　動作を確認する

フォルダの中で、これを1回走らせてください。

```bash
python3 tests/run_all.py
```

`すべて合格` と出れば移行は完了です。
Playwright を入れていない場合は `--quick` を付けてください。

```bash
python3 tests/run_all.py --quick
```

## 手順4　Claude Code に引き継ぐ

そのフォルダで Claude Code を起動し、最初にこう伝えてください。

```
このプロジェクトを引き継ぎます。HANDOVER.md を読んで、
現状と作業ルールを把握してください。
```

以上です。**あなたがやることはこの4手順だけです。**

---

## 補足：やらなくてよいこと

- **ビルド成果物のコミット**は不要です。`assets/diagnostic_tool.html` は
  `scripts/build_tool.py` から生成されます。ただし、そのままブラウザで
  開ける単一ファイルなので、配布用に残しておくと便利です
- **依存パッケージのインストール**は不要です。外部ライブラリを使っていません
  （Playwright は検証用の任意ツール）
- **サーバーの用意**は不要です。生成物は単一HTMLで完結します

---

# 第2節　このプロジェクトは何か

レヴィン（Kurt Lewin）の行動理論をもとにした、132問の自己診断ツールです。
**成果物は単一のHTMLファイル1つ**で、ブラウザで開くだけで動きます。
サーバー通信はせず、回答は端末の外に出ません。

## 中核となる考え方

- **B = f(P, E)** ── 行動は、その人と環境の両方で決まる
- 21項目を測り、能力系11項目と両極性9項目に分けて図示する
- 4つの型を出すが、**型は入り口であって結論ではない**
  （レヴィン自身が1931年に二分法的な分類を批判しているため、
  連続値である2軸の位置を型より先に見せている）

## 絶対に崩してはいけない設計判断

これまでの改修で、理由があって固めた部分です。**変更する前に必ず確認してください。**

| 決めごと | 理由 |
|---|---|
| 本文の数値は**中点スコア（20〜80）だけ**を使う | 素点（1〜6）と混在させると、同じ項目が「5」と「67.6」の2つの数字で出て読み手が混乱する |
| 1つの項目には**1つの呼び名**だけを使う | 以前は項目名・専門用語・両極のラベルで最大4通りの名前になり、別の項目に見えていた |
| 両極性の項目名は**物差しの名前**にする（極の名前にしない） | 「目標を高く設定する／中点よりかなり下」のような自己矛盾を防ぐ |
| 対策は**「まず何をするか」だけ**に書く | 同じ結論を複数の章で繰り返すと、要点が分からなくなる |
| 画面と印刷は**同一のHTML**を使う | 印刷専用の内容を別に作ると、内容がずれて保守できない |
| `ex()`（なぜこの章があるか・理論的背景・図の読み方などのメタ解説）は**すべて既定で畳む**（例外なし） | 実測値・「あなたの場合」・図そのものは `ex()` の外側で常時表示の導入文として別途出力しており、畳んでも要点は消えない。全部開いた状態で出すと読み手の負荷が上がるため、必要な人だけ「解説をすべて開く」で見られるようにする。印刷は `@media print` の `details{display:block!important}` が画面側の `open` の有無に関係なく全文を強制表示するので、既定で畳んでも印刷内容は減らない（`check_print.py` で検証済み）。以前は「21項目の図の読み方」だけ `openOnLoad=true` で例外的に常時展開していたが、上記の印刷側の仕組みが整ったこと、および章の要点をすでに常時表示の導入文で出すようになったことから、この例外は撤回した |
| 「再凍結（refreezing）」という語を使わない | レヴィン自身は "freezing" と書いており、再凍結は後世の語 |
| 回答・結果はブラウザの外に一切送信しない（サーバー通信なし） | GitHub Pagesで一般公開しているため、これが崩れると「共有URL＝データも共有される」という実害につながる |

## 既知の限界（仕様であり、バグではない）

結果画面の「この診断が測れていないこと」に明記済みです。

- 規範集団のデータがないため、他人と比べた高低は言えない
- 21という区分そのものが未検証（因子分析には数百人分の実データが必要）
- 4つの型は、同じ人が2回受けても7割ほどしか一致しない
  （切る前の2軸の位置は9割方一致する）
- E領域は「環境そのもの」ではなく「環境をどう感じるか」を測っており、
  PとEを厳密には切り分けられていない

---

# 第3節　ファイルの役割

## 触ってよいファイル

| ファイル | 役割 |
|---|---|
| `scripts/build_tool.py` | **すべての起点。** 設問文・表示名・HTML・CSS・JSをここから生成する（2,162行） |
| `scripts/score.py` | 保存したJSONを採点し直す。build_tool.py と計算を一致させること |
| `scripts/check_report.py` | 重複・表記ゆれの自動検査 |
| `scripts/check_print.py` | 印刷レイアウトの自動検査 |
| `references/*.md` | 理論の出典、採点仕様、項目バンク、解釈の指針 |
| `SKILL.md` | このツールをスキルとして使う場合の説明 |

## 触ってはいけないファイル（生成物）

| ファイル | 生成元 |
|---|---|
| `assets/diagnostic_tool.html` | `build_tool.py` |
| `assets/items_manifest.json` | 同上 |
| `assets/scale_names.json` | 同上 |
| `index.html` | 同上（`assets/diagnostic_tool.html` と同一内容。存在する場合のみ `build_tool.py` が自動同期する。GitHub Pages配信用） |

**HTMLを直接編集しないでください。** 次のビルドで上書きされます。
必ず `build_tool.py` を直して再生成します。

```bash
python3 scripts/build_tool.py
```

---

# 第4節　作業のやり方

## 基本の流れ

```bash
# 1. build_tool.py を編集する
# 2. 再生成する
python3 scripts/build_tool.py
# 3. 検証する
python3 tests/run_all.py
```

## 検証の内容

`tests/run_all.py` が10項目をまとめて確認します。

| 項目 | 見ているもの |
|---|---|
| ビルド | HTMLが生成できるか |
| JS構文 | 生成されたJavaScriptが壊れていないか |
| 採点スクリプト | score.py が動くか |
| 重複・表記ゆれ | 同じ項目が2通りの呼び名で出ていないか、素点が本文に漏れていないか |
| 印刷レイアウト | @page・改ページ・detailsを開く指定があるか |
| 機能の回帰テスト | 32件（3版の描画、往復保存、妥当性ブロックなど） |
| 理論の要点 | レヴィン由来の23項目が残っているか |
| 章をまたぐ重複 | 同じ文が複数の章に出ていないか |
| 解説の折りたたみが既定で閉じているか | 「この章の図の読み方」を含め、`ex()` はすべて既定で畳まれているか |
| 印刷PDF | 空白ページが出ていないか |

## 注意すべき落とし穴

実際に踏んだものです。

- **`<details>` は `open` がないと印刷で中身が出ません。**
  以前これで全体の47%が印刷から消えていました
- **改ページの指定を二重にすると空白ページが出ます。**
  `h2{break-before:page}` と別の要素の `break-before` が同時に効くと、
  1つの区切りで2回改ページされます
- **`counter(pages)`（総ページ数）はChromiumが未対応です。**
  使うと常に「0 / 0」と表示されます。そのため現在ページ番号は入れていません
- **入れ子の `<details>` は、単純な文字列検索では判定を誤ります。**
  親が閉じていれば、子に `open` があっても表示されません。
  `tests/verify_fold_correct.py` はタグの深さを数えて正しく判定します
- **図の実寸を測るときは、ビューポート幅を印字幅に合わせてください。**
  既定の1280pxで測ると、A4に対して2倍近い数値が出ます
  （印字幅186mm ≒ 703px）

## 変更してよいかの判断

| やろうとしていること | 判断 |
|---|---|
| 設問文の修正 | 可。ただし `ITEM_VERSION` を上げること（過去データとの互換性の記録になる） |
| 表示名の変更 | 可。ただし1項目1呼び名の原則を守る |
| 新しい指標の追加 | 慎重に。既存指標と式が重なっていないか確認する（過去に3つ廃止した） |
| 型を増やす／減らす | 非推奨。レヴィンの立場と衝突するため、型はすでに格下げ済み |
| 素点を本文に出す | 不可。中点スコアに統一している |
| 印刷専用の構成を作る | 不可。画面と同一にする方針 |

---

# 第5節　これまでの経緯（要約）

Claude Code が背景を理解するための時系列です。

1. **初期**：132問・21項目・8指標・4類型のツールとして作成
2. **監査**：21件の問題を検出。型判定が49%も「中間型」になる欠陥、
   判定帯の膨張（「非常に高い」が14%出現）などを発見し修正
3. **理論の再調査**：レヴィン（1931）が二分法的分類を批判していたことが判明。
   同じ論文を葛藤3類型の出典にしていたため、型を主役から降格
4. **表現の平易化**：やさしい日本語の指針に沿って書き換え。
   その後「子供向けすぎる」との指摘を受け、成人向けの語調に再調整
5. **重複の整理**：同じ項目が最大4通りの呼び名・2種類の数値で出ていた問題を解消。
   指標を9つから6つへ統廃合（式が重なっていた3つを廃止）
6. **印刷対応**：折りたたみが印刷されず47%が消える問題を修正。
   一度は印刷専用レイアウトを作ったが、方針変更により画面と統一

**この経緯から言えること**：このプロジェクトでは
「機械的な検査で見つかる問題」と「実際に出力を見ないと分からない問題」の
両方が繰り返し出てきました。変更したら必ず両方やってください。

---

# 第6節　次にやるとよいこと（未着手）

優先度順です。着手は任意です。

## 1. 公開（ホスティング）── 完了

GitHub Pages で公開済み（リポジトリ直下の `index.html` を配信）。

## 2. 実データによる検証 ── 実施しない方針（意思決定済み）

因子分析・信頼性係数（クロンバックのα）の算出、中点スコアの標準得点化には
数百人分の実データが要る。**検証用データの収集は行わない方針とすることで
決定済み。** これにより「21という区分そのものが未検証」「中点スコアは
理論上の暫定値」という既知の限界（126行目付近）は、一時的な状態ではなく
**恒久的な設計方針**として扱う。「本ツールは測定器ではなく振り返りの道具」
という位置づけを崩さないこと。

心理統計・尺度開発の専門家への依頼も、この決定に伴い保留とする
（実データがない状態では依頼しても検証のしようがないため）。

## 3. データを必要としない専門家への依頼

実データがなくても進められる品質向上として、以下を優先順に検討する。

1. **臨床心理士・公認心理師／産業組織心理学者** ── 解釈文・免責文言の
   リスクレビュー。「病気の診断・向き不向きの判定・採用や人事評価には
   使わないでください」という現状の免責が十分か、断定的すぎる言い回しが
   残っていないかを確認してもらう。
2. **UXライター／編集者** ── 対話ベースでの磨き込みが一巡し構成が
   安定してきたタイミングで、通しで読む専門の目を一度入れる。
3. **UIデザイナー** ── 情報の視覚的な優先順位づけ、図表のビジュアル、
   タイポグラフィなど、機能面が整った今だからこそ効果が出る領域。

### 実在の専門家が見つかるまでの一次レビュー（Claudeによる代行、実施済み）

人を探す前段階として、Claude自身が上記1〜3の視点で結果画面をレビューし、
見つかった問題は直接修正済み（コミット履歴参照）。

- **1の視点**：「回答の確認」冒頭の比喩が臨床検査（医療行為）に例えており、
  末尾の「学術的に検証された心理検査ではない」という免責と矛盾していたため、
  非医療の比喩に差し替えた。また慢性疲労・燃え尽きに近い記述があるのに
  専門家相談への導線がなかったため、末尾の注記に1文追加した。
- **2の視点**：「行動の式」P領域の本文で、能力系2項目のうち高いほうを
  絶対値に関係なく「強み」と呼んでいたバグ（両方が40未満でも高いほうが
  強み表示になっていた）を修正。`band()`の基準（60以上）に合わせた。
- **3の視点**：CSSを精読した限りではコントラスト比・フォーカス表示・
  `prefers-reduced-motion`/`prefers-contrast`対応・44px以上のタップ領域など
  アクセシビリティ基盤はすでに水準が高い。**ただしブラウザ拡張が未接続で
  実機のスクリーンショット確認はできておらず**、見出しカードにごく薄い
  box-shadowを足す程度の低リスクな調整にとどめた。実機での視覚確認と、
  情報の優先順位づけ・タイポグラフィなど本格的なビジュアル面の判断は、
  引き続き人のUIデザイナーに依頼する価値がある領域として残っている。

これらはClaudeによる代行レビューであり、実在の専門家（特に1・3）による
確認の代わりにはならない。人が見つかった際は、上記の指摘一覧を出発点として
渡せる。

### ブラウザ実機での視覚確認（2回目、実施済み）

上記の1回目レビューではブラウザ拡張が未接続で実機確認ができなかったが、
その後の回でユーザーの許可を得てブラウザ拡張を接続し、ローカルHTTPサーバー
（`py -m http.server`）経由で`assets/diagnostic_tool.html`を実際に開き、
JavaScriptで132問分の回答を機械的に埋めて`showResult()`を実行、実際のCSSが
かかった結果画面をスクロールしながら確認した。折りたたみの開閉（`解説を
すべて開く`ボタン、個別の`ex()`要素）も実クリックで動作確認した。

- 確認した限り、見出しカードのbox-shadow・強み/苦手カードの色分け・対策
  カード（対策1/2/3）の行分けされた構造・バッジ類のコントラストは、いずれも
  実際のレンダリングで問題なく視認できた。折りたたみの開閉も正しく動作した。
- 唯一見つかった改善点：「なぜそうなるのか　── あなたの行動の式」章の
  P領域の説明文（`function equation`の`pTxt`、フルプロファイル版）で、
  両極性3項目の位置づけ文と能力系2項目の強み・伸びしろ文が改行なしで連結
  されており、実際の幅で見ると壁のような段落になっていた。2文の間に
  `<br>`を1つ挿入し、視覚的に分けた（文章の内容・語彙は変更していない）。
- **今回もブラウザで確認できなかったこと**：モバイル幅（狭い画面）での
  表示は、`resize_window`ツールの変更が実ブラウザウィンドウに反映されず
  （`window.innerWidth`が変わらなかった）確認できなかった。ダークモードは
  ページ側にテーマ切り替えUIがなく、`prefers-color-scheme`はOS設定連動の
  ため、JavaScriptから強制的に切り替えて確認することができなかった。
  これらは「拡張未接続」とは別の制約として残っている。CSSソース上の
  レスポンシブ対応・`prefers-color-scheme:dark`ルールの存在自体は1回目の
  レビューで確認済み。

## 4. 再測定の基準の実測 ── 実施しない方針（意思決定済み）

いまは「5点未満の差は変化とみなさない」としていますが、これは測定誤差から
求めた値ではなく暫定値です。正しい基準の実測には同じ人に短期間で2回実施した
データが要りますが、上記2のとおりデータ収集自体を行わない方針のため、
この暫定値も恒久的な扱いとする。

## 5. 印刷のページ番号

`counter(pages)` が使えないため未実装です。
JavaScriptで総ページ数を数えて埋め込む方法はありますが、
印刷ダイアログの設定（用紙サイズ・倍率）で変わるため確実ではありません。

---

# 第7節　困ったときの確認先

| 知りたいこと | 見るファイル |
|---|---|
| ある設問が何を測っているか | `references/item_bank.md` |
| 指標の計算式と閾値の根拠 | `references/scoring.md` |
| レヴィン理論の出典と誤りやすい点 | `references/theory.md` |
| 結果の書き方・言葉の選び方 | `references/interpretation.md` |
| 検査が何を見ているか | `scripts/check_report.py` と `scripts/check_print.py` の冒頭 |

`build_tool.py` の中にも、判断の理由をコメントで残しています。
「なぜこうなっているのか」は、たいていコード内に書いてあります。
